name: nexbar2-staging-manual
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  staging-note:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automated staging is disabled; active development is on wip/nexbar2-v1."
