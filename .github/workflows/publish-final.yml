name: cdc-preview-red
on:
  push:
    branches: [upload/nexbar2-final]
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: wip/nexbar2-v1
      - name: Confirm stale HID preview fails CDC contract
        run: |
          npm ci
          npm test
