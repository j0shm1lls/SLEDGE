name: diagnostics-cdc-red
on:
  push:
    branches: [upload/nexbar2-final]
permissions:
  contents: read
jobs:
  red:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: wip/nexbar2-v1
      - name: Confirm stale diagnostics label fails CDC contract
        run: |
          npm ci
          npm test
